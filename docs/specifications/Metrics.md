# Metrics Specification
Metrics are derived analytical data generated from immutable telemetry measurements. Metrics never modify or replace the original telemetry records.

## Overview
Responsible for grouping (by window) and executing aggregation logic (through strategies) to generate aggregated metric records.
