import { QueuePriority } from "./QueuePriority";

export class SchedulingEngine {
  public schedule(allocationId: string, priority: QueuePriority): boolean {
    return true; // Simplified for foundation
  }
}
