# PROから次期AIへの引き継ぎ事項 (更新: 2026-05-24)

次回の担当AIへ。以下のコンテキストとルールを読み込み、これまでの作業状態を正確に引き継いでください。現在、本システムは「初期費用100万円・月額10万円以上の超プレミアムSaaS」として極限のブラッシュアップを行っています。

## 1. 直近の完了タスク（進捗率の加重平均化とオシャレな表示）
本日（Pro）のセッションでは、進捗率の計算ロジックと表示仕様をブラッシュアップしました。

- **GAS API のデプロイ完了**:
  - `clasp push` を行い、APIレスポンス (`getAppData`) に各地区の `done` / `total` 情報と全体の `stats` 合計を追加しました。
- **加重平均による全体進捗率の算出**:
  - `app.js` の `updateStats` を改修し、単純平均ではなく全体の完了数と総数の比率から正確な進捗率（％）を計算するように変更しました。
- **ヘッダー＆地区カードのUIオシャレ表示化**:
  - ヘッダーの進捗（％）表示の隣に、控えめな薄いグレー (`white/40` 10px) で `(done/total)` 件数を表示させました。
  - 各地区カードの進捗パーセントの横にも、薄いグレー (`white/20`) で `(done/total)` 件数を追加し、全体の状況が俯瞰できるようにしました。
  - いずれも `git push` が成功し、本番環境（GitHub Pages）にデプロイ済みです。

## 2. 次回（Flash等）へのタスク
- **更なる画面レイアウト・スクロール調整**:
  - ログイン後の各内部画面（地区詳細、設定/IDカード）について、スクロール時の挙動や下部ナビゲーションとの重なり、プレミアムなガラスUIが完璧に調和しているかを継続して調整・検証してください。
- **管理者画面（manager.html）の調整**:
  - 管理者ダッシュボードのKPIパネルや操作コマンドのガラスUI最適化も、必要に応じて実施してください。

## 3. アーキテクチャの基本構造（絶対に忘れないこと）
- **フロントエンド（見た目）**: GitHub Pages (`index.html`, `manager.html`)。修正後はローカルコミットし、岩佐さんに `git push` を依頼。
- **バックエンド（裏側・DB）**: GAS (`v2_api.gs` 他)。修正後は `clasp push` & `clasp deploy -i [ID]`。
- 両者は完全に分離されています。

## 4. 絶対遵守ルール (from AGENTS.md)
**🚨 承認なき実行の絶対禁止（提案 → 承認 → 実行のフロー）**
AIは提案のみを行い、岩佐さんの明確な「承認(Yes/OK/GO)」なしにいかなるファイル操作（ツールを使った編集・置換）も実行してはならない。「スクロールはやめるか！」などの会話のニュアンスだけで勝手に実行し、厳重注意を受けたばかりです。次期担当AIは**絶対に**この確認フローを厳守すること。
