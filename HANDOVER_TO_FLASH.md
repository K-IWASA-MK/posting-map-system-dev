# 開発引き継ぎ事項 (更新: 2026-05-26 v287)

次回の担当AIへ。以下のコンテキストを読み込み、これまでの開発履歴と現状を確認して作業を開始してください。

---

## 1. プロジェクト概要

- **フロントエンド**: GitHub Pages (`area-management.github.io/posting-map-system`)
- **バックエンド**: Google Apps Script (GAS) API
- **現在のキャッシュバスター**: `v287`
- **Gitブランチ**: `main`（最新コミットまでプッシュ完了済み）

---

## 2. これまでに完了した重要な変更点

### ① ボトムナビの3種類化
- `📍 地区` → `🗺️ エリア`、`🏆 ランキング`、`👤 ID` の3ボタン構成。
- `app.js` のアクティブ状態制御 (`opacity 1/0.3`) の3ボタン対応。
- `page-ranking` の追加。

### ② 市・自治体ごとのドリルダウン（2層構造）
- **第1層（市一覧）**: 「エリア」ボタンをタップすると、四日市市・鈴鹿市・亀山市などの自治体単位のカードが表示される。
- **第2層（エリアシート一覧）**: 市カードをタップすると、その市に属するエリアシートが表示される。
- **タイトル文字の削除**: 2枚目のエリアシート一覧（第2層）から redundant な「◯◯市 エリアシート」のテキストを削除し、極限まで削ぎ落としたミニマルな画面に統一。

### ③ 戻るボタンの改善
- **上下への配置**: 2枚目のエリアシート一覧（第2層）にて、上部および下部の2箇所に戻るボタン「‹」を配置（沈み込む `scale(0.92)` アニメーション付き）。
- **表示件数の動的判定と余白最適化**: 
  - `style.css` の `#page-areas` 側で持っている `160px` の下部余白と競合しないよう、ボタン側の `pb-32` を削除。
  - リスト内のカードが **3件以下**（スクロール不要な短いリスト）の場合は、下部の戻るボタンを自動的に非表示にして無駄なスクロールを完全に排除。
  - カードが **4件以上**（スクロールが必要な長いリスト）の場合のみ、下部に戻るボタンを自動表示。

### ④ ジャイロセンサー連動機能の完全廃止（差し戻し）
- **決定事項**: スマホの傾き検知およびアクセス許可要求（iOS 13+ のポップアップなど）は動作が不安定になる原因となるため、ユーザーのフィードバックに基づき**完全に削除**しました。
- 現在の ID カード（`👤 ID`）は、追加のダイアログやバグの発生しない極めて安定した静的デザイン表示に復元されています。

---

## 3. デザイン保護・固定ルール（変更禁止事項）

- **上部ヘッダー**: `px-6` の横幅および `gap-3` レイアウトが確定版（変更禁止）。
- **ONLINEインジケーター**: `animate-soft-pulse` による1.5秒の微パルスが確定版（変更禁止）。
- **下部ナビゲーション**: 3ボタン構成（🗺️ エリア、🏆 ランキング、👤 ID）が確定版（変更禁止）。
- **IDカード外側ラッパー**: `pt-2 pb-0 px-4 flex flex-col items-center` が確定版（変更禁止）。
- **設定画面スクロール固定**: スクロール不可の固定レイアウト（変更禁止）。
- **キャッシュバスター**: 現在 `v287`。フロントエンドを変更する際は必ずインクリメントしてキャッシュバスターを更新してください。
