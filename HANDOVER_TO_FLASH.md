# 開発引き継ぎ事項 (更新: 2026-05-25)

次回の担当AIへ。以下のコンテキストを読み込み、前回のタスク範囲の変更点のみを確認して作業を再開してください。

> [!IMPORTANT]
> **再開時の検証ルール (AGENTS.mdより)**:
> セッション再開時は、前回実施された変更点の確認作業のみを行ってください。システム全体に及ぶ網羅的な確認や再テストは不要です。

---

## 1. 今回完了したタスクと変更点

フロントエンド（GitHub Pages）・バックエンド（GAS API）ともに適用・デプロイ済みです。

### ① スクロール状態の自動保持・復元 (`app.js`, `render.js`)
- 各画面遷移（地区一覧 ⇄ 地区詳細 ⇄ 設定）の前後でスクロール位置をキャッシュ・復元する仕組みを実装しました。
- 新しい地区詳細を開く際は、スクロール位置を自動的に `0`（最上部）にクリアします。
- 設定・IDカード画面で、極小デバイスやキーボード表示時にコンテンツが切れる問題を解消するため、`overflow-y: auto` でスクロール可能にしました。

### ② 重複するヘッダータイトルの削除と「全体進捗バッジ」の刷新 (`index.html`, `app.js`)
- LINE WebViewのヘッダーにタイトルが表示されるため、Webアプリ内のテキストロゴ「POSTING MAP」を完全に削除して縦スペースを広げました。
- 簡素な進捗表示を、ヘッダー幅いっぱいのプレミアムなステータスバー型バッジ「**全体進捗  (  5/651) 🟢  1%**」へと刷新しました。
- 件数部分には等幅フォント（`font-mono`）と `whitespace-pre` を適用し、全体数の桁数（3桁や4桁など）に合わせて、完了数の左側を動的に半角スペースで右寄せパディング（`padStart`）するロジックを実装しました。これにより、件数増減時も `/651` などの位置がガタつかず固定されます。
- 同期状態ドットとパーセントの間に、ゆとりある余白（`ml-5`）を設定しました。

### ③ 管理者画面（`manager.html`）の進捗率整合
- 全体カバー率（進捗率）の計算を、単純平均から「完了数 / 総数」による加重平均に変更し、クライアントアプリと整合させました。
- ダッシュボードのコマンドログへ、全体数に対する完了数の詳細（例: `Total: 5/651 points`）を出力するように拡張しました。

### ④ バックエンド（GAS API）のフォールバック集計実装 (`scripts/v2_api.gs`)
- 接続先URLを現在のシート用のデプロイID（`AKfycbyFoJ...`）に統一し、`clasp deploy` で更新しました。
- GAS側のキャッシュシート（`__SYSTEM_CACHE__`）が空、または未構築の場合でも、各地区シートの物理行数（`s.getLastRow() - 1`）をその場でカウントしてフォールバックするロジックを追加したことで、全体数が `0` ではなく実際の総数（例: `651`）として常に正しく取得できるようになりました。

### ⑤ 設定画面IDカードの垂直位置決定
- スクロール無しの固定状態で、ヘッダー ⇄ タイトルラベル（距離A）と タイトルラベル ⇄ IDカード本体（距離B）の余白を共に「40px（`pt-10` と `mb-10`）」で対称（均等）にする美しいグリッド配置を適用しました。
- トリッキーな `translate-y` によるずらしを廃止し、`justify-start` で上から安定した位置で整列させています。

---

## 2. デザイン保護・固定ルール（次回の開発者へ）
- **上部ヘッダーの変更禁止**: 全体進捗バッジを含む `<header>` 部分は完成しています。二度と位置や余白を変更しないでください。
- **IDカードレイアウトの維持**: スクロール不可の状態で `pt-10` & `mb-10` に設定したレイアウトが最終決定版（上下均等バランス）です。これ以上のレイアウト変更は行わないでください。
