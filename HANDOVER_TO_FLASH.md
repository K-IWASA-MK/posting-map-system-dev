# PROから次期AIへの引き継ぎ事項 (更新: 2026-05-17)

次回の担当AIへ。以下のコンテキストとルールを読み込み、これまでの作業状態を正確に引き継いでください。現在、本システムは「初期費用100万円・月額10万円以上の超プレミアムSaaS」として極限のブラッシュアップを行っています。

## 1. 直近の完了タスク（デザイン・レイアウトの究極最適化）
本日（Pro）のセッションでは、主に「ブラウザ特有の安っぽさ（被りや余分なスクロール）の排除」と「ガラスUIの極限追求」を行いました。現在はまだ「1枚目の画面（Splash/Gateway画面）」までが完了した状態です。

- **超微弱グラデーションの制定**: 
  - `AGENTS.md` の Card Style を更新。すべてのカードや枠線には、単色ではなく `linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.008))` の極薄グラデーションと、微弱な青グロー (`box-shadow`) を使用し、物理的なガラスの質感を強制すること。
- **100dvhと均等余白による「被り・スクロール」の完全排除**:
  - `index.html` および `manager.html` の1枚目（Gateway画面）において、Safariブラウザの上下メニューバーと要素が被る問題を解消。
  - `fixed inset-0` と過剰な下余白（`pb-24`等）を廃止し、**`h-[100dvh]` と上下均等な余白（`p-6 pb-6` 等）**を採用。これによりブラウザ時は安全に縮み、PWA時は全画面に拡張される完全レスポンシブを実現。
- **スクロールのJSロック**:
  - `index.html` の `switchPage` にて、1画面固定にしたい設定画面（登録やIDカード）を開いた時のみ、コンテナの大枠下余白（`pb-64`）をJSで動的に除去。これにより「無駄に下にスクロールできてしまう」ブラウザ感を撲滅。

## 2. 次回（Flash等）へのタスク
本日は「1枚目の画面（Gateway・登録画面）」のブラウザ被り解消までが完了しました。
**次回は、以降の画面（ログイン後の内部画面や、ADMIN PANELの内部画面など）についても、同様に「被らないか」「無駄なスクロールが発生していないか」「100万円のSaaSにふさわしい極上のガラスUIが適用されているか」のレイアウト調整を進める必要があります。**

## 3. アーキテクチャの基本構造（絶対に忘れないこと）
- **フロントエンド（見た目）**: GitHub Pages (`index.html`, `manager.html`)。修正後はローカルコミットし、岩佐さんに `git push` を依頼。
- **バックエンド（裏側・DB）**: GAS (`v2_api.gs` 他)。修正後は `clasp push` & `clasp deploy -i [ID]`。
- 両者は完全に分離されています。

## 4. 絶対遵守ルール (from AGENTS.md)
**🚨 承認なき実行の絶対禁止（提案 → 承認 → 実行のフロー）**
AIは提案のみを行い、岩佐さんの明確な「承認(Yes/OK/GO)」なしにいかなるファイル操作（ツールを使った編集・置換）も実行してはならない。「スクロールはやめるか！」などの会話のニュアンスだけで勝手に実行し、厳重注意を受けたばかりです。次期担当AIは**絶対に**この確認フローを厳守すること。
