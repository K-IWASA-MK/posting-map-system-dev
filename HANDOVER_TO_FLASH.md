# 開発引き継ぎ事項 (更新: 2026-05-26)

次回の担当AIへ。以下のコンテキストを読み込み、前回のタスク範囲の変更点のみを確認して作業を再開してください。

> [!IMPORTANT]
> **再開時の検証ルール (AGENTS.mdより)**:
> セッション再開時は、前回実施された変更点の確認作業のみを行ってください。システム全体に及ぶ網羅的な確認や再テストは不要です。

---

## 1. 今回完了したタスクと変更点

フロントエンド（GitHub Pages）へ適用・デプロイ済みです。キャッシュバスターは **v259** です。

### ① 設定画面IDカードの縦位置を上に調整 (`render.js`)
- Card 2（IDカード表示）の外側ラッパーのパディングを `py-6` → `pt-2 pb-0` に変更しました。
- これにより「公式配布員 IDカード」ラベル・バッジ・カード本体の全ブロックが上方向に移動しました。
- ヘッダーおよびボトムナビの位置は変更していません。

### ② IDカードのレイアウトを旧デザインに復元 (`render.js`)
- タイトル行を縦並び2行から**横並び（横一列）**に戻しました：「公式配布員 IDカード　[S 001]」
- IDバッジを青い丸角ピル型（`rounded-full`、ブルー背景）スタイルに復元しました。
- IDは `S001` → `S 001` のようにアルファベットと数字の間にスペースを入れてフォーマットしています。
- カード内レイアウトを `justify-between` の3段構成（上:AUTHORIZED STAFF / 中:アバター+名前 / 下:支部名+FIELD OPERATIONS）に復元しました。
- **「MIE-02 支部」（支部名）** をカード内に復活させました。

### ③ branchName の保存処理を復元 (`app.js`)
- `getAppData` APIレスポンスの `branchName`（スプレッドシートのタイトルから取得）を `localStorage.setItem('branch_name', ...)` で保存する処理を追加しました。
- `render.js` 側で `localStorage.getItem('branch_name')` を読み取り、`MIE-02 支部` のように表示します。
- `branch_name` が未取得の場合は支部名を非表示にします（フォールバックのハードコーディングなし）。

---

## 2. デザイン保護・固定ルール（次回の開発者へ）
- **上部ヘッダーの変更禁止**: 全体進捗バッジを含む `<header>` 部分は完成しています。二度と位置や余白を変更しないでください。
- **IDカード外側ラッパー**: `pt-2 pb-0 px-4 flex flex-col items-center` が確定版です。縦位置の再調整は不要です。
- **カード内レイアウト**: `justify-between` の3段構成（top/middle/bottom）が確定版です。
- **branchName**: GAS APIの `getAppData` レスポンスから動的に取得・保存される設計です。ハードコーディングしないでください。
- **キャッシュバスター**: 現在 `v259`。フロントエンドファイルを変更する際は必ず `index.html` のバージョン番号を上げてください。
