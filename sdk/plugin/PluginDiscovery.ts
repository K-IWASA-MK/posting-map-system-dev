import { IAIOSManifest } from '../contracts';

export class PluginDiscovery {
  public async discover(directoryPath: string): Promise<IAIOSManifest[]> {
    // Placeholder for reading manifest.json from plugin directories
    return [];
  }
}
