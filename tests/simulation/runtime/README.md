# Simulation Runtime

This directory contains the active simulation runtime used by:

- Simulation Tests
- Pre-Commit Hooks
- Validation Infrastructure

Historical implementations are archived under:

`knowledge/legacy/`

The active runtime must never depend on `knowledge/legacy/`.
